# Evals

- Given an unrelated marketing article, then decision is `reject` or `reference_only`.
- Given a useful CLI testing pattern, then proposal includes a target skill and a testable eval.
- Given an external repo script, then proposal warns not to execute it.
